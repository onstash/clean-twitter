{
  "name": "clean-twitter",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Santosh Venkatraman <santosh.venk@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && parcel build src/content.js --no-source-maps && parcel build src/background.js --no-source-maps && cp manifest.json dist/. && cp -r images/. dist/images"
  }
}
